name: Build and Push Docker Image

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Releases
        uses: andrewthetechie/gha-clone-releases@v1.0.0
        with:
          token: ${{ secrets.THIS_PAT }}
          repo: cloudquery/cloudquery
  