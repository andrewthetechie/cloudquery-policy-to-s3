name: Clone Releases from Cloudquery/Cloudquery

on:
  schedule:
    # right now we run hourly. I picked 4 after arbitrarily. 4 is a nice number
    - cron: '4 * * * *'
  workflow_dispatch:

jobs:
  clone:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Releases
        uses: andrewthetechie/gha-clone-releases@v1.1.0
        with:
          token: ${{ secrets.THIS_PAT }}
          repo: cloudquery/cloudquery
          skip_draft: true
          skip_prerelease: true
          limit: 10